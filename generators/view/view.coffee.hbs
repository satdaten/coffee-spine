template = require 'templates/{{name}}'

module.exports = class {{#camelize}}{{name}}{{/camelize}}View extends Spine.Controller
  className: '{{name}}-view'
  _template: template
  
  _render: =>
    @html @_template()